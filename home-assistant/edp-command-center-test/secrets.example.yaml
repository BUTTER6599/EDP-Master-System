# Copy this key/value into Home Assistant secrets.yaml.
# Replace the URL only after the Apps Script TEST deployment is created.
edp_ha_bridge_url: "https://script.google.com/macros/s/REPLACE_WITH_TEST_DEPLOYMENT_ID/exec"
