# Copy this key/value into Home Assistant secrets.yaml.
# Generate a long random key and also save the same value as the Apps Script
# Script Property named EDP_BRIDGE_KEY.
edp_ha_bridge_url: "https://script.google.com/macros/s/REPLACE_WITH_TEST_DEPLOYMENT_ID/exec?key=REPLACE_WITH_LONG_RANDOM_KEY"
